# Client application

Useful commands:

```sh
yarn start
yarn build
yarn lint
yarn lint-fix
yarn deploy
```

## Running locally

Either run `yarn start` or use the container instructions on the main README.
