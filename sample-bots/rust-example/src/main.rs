fn main() {
    println!("Hello, world!");

    // // Optionally Read the input from stdin.
    // let mut input = String::new();
    // std::io::stdin().read_line(&mut input).unwrap();
    // // Parse json
    // let json: serde_json::Value = serde_json::from_str(&input).unwrap();
    // println!("json: {}", json.get("botname").unwrap());
    // println!("Opponent: {}", json.get("opponent").unwrap());

    // Output your play as the last line.
    println!("paper");
}
